# Cookie Backend 2026

Установка и запуск, будет работать на localhost:8000/api/v1
```bash
git clone https://github.com/yaroniks/Cookie2026.git
cd CookieBackend2026

git pull origin main
docker compose up --build -d
```
