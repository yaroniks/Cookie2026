# Cookie frontend 2026:

Установка и запуск, будет работать на localhost:3000
```bash
git clone https://github.com/yaroniks/CookieBackend2026.git
cd CookieBackend2026

git pull origin main
docker compose up --build -d
```